cd '' => should keep you at the same directory > to be fixed in parser
unset HOME then cd segfault; > fixed
multiple expantions to be checked
env abnormal behavior
exit long long : should not exit
if path unset, builtins MAJ should print no such file or directory
export HOME=$USER env var do not expand
mkdir folde cd folder rm folder cd . pwd
digit or char in export
 echo -n -n -n test tout should skip -n

cd '' => should keep you at the same directory > SENDING NULL NOW RECEIVING THE NO SUCH FILE OR DIRECTORY PROBLEM -> fixed
unset HOME then cd segfault; > fixed
multiple expantions -> fixed
multiple expantions back to back -> fixed might need more testing -> fixed
if path unset, builtins MAJ should print no such file or directory // should be ignored since their binaries are ignored from the start
digit or char in export -> fixed
echo -n -n -n test tout should skip -n -> fixed
export HOME=$USER env var do not expand -> FIXED
mkdir folde cd folder rm folder cd . pwd -> fixed -> oldpwd to be tested
unset first env variable segfault a weld l97ba -> fixed a wld l97ba
cat < Makefile Segfault -> fixed
echo '' '' '' | cat -e should print spaces -> fixed
exit long long : should not exit -> fixed
echo < file teasdasd segfault -> fixed
fix is directory error msg when exec a dir -> fixed
env abnormal behavior -> fixed
echo "$TEST=$TEST=lol$USER" >> ==lolabiari ->fixed
echo "$" -> fixed
\ triggers heap buffer overflow -> fixed
remove "" and '' in export "var"/'var'=test ->fixed (treating backslashes as normal characters from now on)
echo > < >> print syntax error a l7mar o > bo7dha and < bo7dha layn3lzxamlbouk, zid ta "chi 9lawa" > -> fixed I GUESS needs more testing
cat > file2>file3 -> fix code after heredoc removal ->fixed
when expanding only empty variable/s send NULL -> fixed
<< stop cat -e ---> segfault -> heredoc to be reimplemented -> fixed
for later //return values ($?) ?????? -> fixed
echo "" double free
echo djsfdfdfkdsj >testt.txt < testt.txt -> fixed
msh$: TheNameOfAnExistingFolder ======> FIXED
COMMAND NOT FOUND ======> FIXED
EXIT CODE SHOULD BE 127 NOT 126 (your output is ==> FOLDER : is a directory) -> not true ======> FIXED
unset PATH -> ls -> echo $? => it returns 2 expected 127 ======> FIXED
unset PATH -> ./TODO -> echo $? => it returns 13 expected 126 ======> FIXED
unset PATH -> /libft -> echo $? => it returns 2 expected 127 ======> FIXED
(you must check if the command contains slashes because if it does you will need to ) ======> FIXED
 (by eel-orch)
ls | no error ==> fixed
========================================================================================================================================
//exec relative path to be checked, ./file exmple
cat << filee << fileee <file > second file name get only first character parsed
fix _ env var in parser, to be filled with last arg of last command or last simple command   and expanded in parser
https://pubs.opengroup.org/onlinepubs/009604499/utilities/xcu_chap02.html#tag_02_09_01_01
---------------------


